[project]
name = "shared-storage"
version = "0.1.0"
description = "JsonStore: escrita atomica, lock por caminho e indice. Nao importa ninguem."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/shared_storage"]
